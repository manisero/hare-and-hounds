- ContextManager should keep Activities stack - this will allow to finish the application programatically (invoking Finish method on every activity)

- ViewModel should be able to finish the activity

- PositionTracking: PositionDataList should ignore position addition if the position being added is near the last position on the list

- SpaceGraphics view should work in landscape mode

- CameraPreview should work in portrait mode
