PositionData: Latitude and Longitude are displayed in wrong order (co clue why...)

SpaceGraphicsView does now work when defined in xml layout (hot-fixed - the constructor still needs investigation)

HareAndHounds: Hounds reports their location too often (at least on real device)

SpaceGraphics: OrientationProvider does not handle Landscape mode